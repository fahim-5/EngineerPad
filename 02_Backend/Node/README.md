### **Essential Node.js Crash Course (Pre-Express)**  

#### **1. Node.js Fundamentals**  
- What is Node.js?  
- How Node.js differs from browser JavaScript  
- Global objects (`process`, `global`, `__dirname`, `__filename`)  

#### **2. Core Modules**  
- `http` (Create server, handle requests)  
- `fs` (File system operations)  
- `path` (Path handling)  
- `events` (EventEmitter basics)  

#### **3. NPM & Packages**  
- Installing packages (`npm install`)  
- `package.json` basics  
- CommonJS (`require` vs `import`)  

#### **4. Asynchronous JavaScript**  
- Callbacks → Promises → Async/Await  
- Error handling in async code  
- `Promise.all()` for parallel tasks  

#### **5. Build a Basic HTTP Server (Without Express)**  
- Handle routes manually  
- Parse URLs and query params  
- Send JSON responses  

#### **6. Middleware Concept**  
- What is middleware?  
- `next()` function and chaining  
- Simulate middleware in vanilla Node.js  

---

### **Next Step After This**  
Once comfortable with:  
1. Creating a server with `http`  
2. Handling async code  
3. Using `require` and npm  

→ **Start Express.js immediately**.